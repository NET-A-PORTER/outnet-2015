${{name}}IconsMap: (
  {{#shapes}}
    {{ base }}: (
      coordinates: {{ position.relative.xy }},
      width: {{ width.inner }}px,
      height: {{ height.inner }}px,
    ),
  {{/shapes}}
);

${{name}}Icons: map-keys(${{name}}IconsMap);
